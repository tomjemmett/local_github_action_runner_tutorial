name: R Hello World

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: R Hello World
        run: |
          cd
          dir
        shell: cmd
