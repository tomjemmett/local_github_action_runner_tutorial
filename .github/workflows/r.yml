name: R Hello World

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: R Hello World
        run: |
          run_hello_world.cmd
        shell: cmd
